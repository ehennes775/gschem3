namespace Geda3
{
    /**
     * Represents the dash type for lines
     */
    enum DashType
    {
        SOLID,
        DOTTED,
        DASHED,
        CENTER,
        PHANTOM,
        COUNT
    }
}
